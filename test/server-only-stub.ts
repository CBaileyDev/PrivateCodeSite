// Test stub for the `server-only` marker package, which intentionally throws
// when imported outside React Server Component bundling. Tests run in Node, so
// we alias it to this no-op (see vitest.config.ts).
export {};
